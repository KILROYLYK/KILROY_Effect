/**
 * Public
 */
import { Base } from '../../../_Base/js/window';

/**
 * 全局参数
 */
export const px = Base.isPSB.platform() === 'PC' ? 1 : 2;
export const src = {
    img: '',
    json: '',
    media: ''
};
